apiVersion: v1
kind: Service
metadata:
  name: fmbackendservice
spec:
  selector:
    app: fmbackendlabel
  type: LoadBalancer
  ports:
    - name: fmbackendport
      port: 80
      targetPort: 8000
  externalIPs:
    - 192.168.49.2